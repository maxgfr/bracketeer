{
  "$schema": "https://github.com/maxgfr/bracketeer",
  "name": "Start from nothing",
  "summary": "A plain knockout with every default left alone. Take it apart on the Rules tab and build whatever you actually run.",
  "signature": "single elimination · seeded",
  "config": {
    "stages": [
      {
        "kind": "single_elimination",
        "id": "main",
        "name": "Main draw"
      }
    ]
  }
}
